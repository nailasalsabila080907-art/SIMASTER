@extends('layouts.app')
@section('title','Pengguna Sistem')
@section('content')
<div class="w-full">@if(session('sukses'))<div class="mb-5 rounded-xl bg-green-50 border border-green-200 text-green-700 px-5 py-4 text-sm">{{session('sukses')}}</div>@endif @if(session('gagal'))<div class="mb-5 rounded-xl bg-red-50 border border-red-200 text-red-700 px-5 py-4 text-sm">{{session('gagal')}}</div>@endif
<div class="mb-5"><h2 class="font-display text-xl" style="color:var(--navy)">Pengguna Sistem</h2><p class="text-sm mt-1" style="color:var(--ink-muted)">Kelola username, role, status, dan password akun pegawai.</p></div>
<div class="bg-white rounded-2xl border border-gray-200 overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-gray-50"><tr class="text-left"><th class="px-5 py-4">Pengguna</th><th class="px-5 py-4">Jabatan</th><th class="px-5 py-4">Role</th><th class="px-5 py-4">Status</th><th class="px-5 py-4 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($pengguna as $u)<tr><td class="px-5 py-4"><div class="font-medium">{{$u->username}}</div><div class="text-xs mt-1" style="color:var(--ink-muted)">{{$u->pegawai->nama_lengkap ?? '-'}}</div></td><td class="px-5 py-4">{{$u->pegawai->jabatan->nama_jabatan ?? '-'}}</td><td class="px-5 py-4">{{ucwords(str_replace('_',' ',$u->role))}}</td><td class="px-5 py-4">{{$u->status}}</td><td class="px-5 py-4 text-right"><a href="{{route('pengguna.edit',$u)}}" class="text-xs mr-3" style="color:var(--navy)">Ubah</a>@if($u->id_user!==auth()->id())<form class="inline" method="POST" action="{{route('pengguna.destroy',$u)}}" onsubmit="return confirm('Hapus akun ini?')">@csrf @method('DELETE')<button class="text-xs text-red-600">Hapus</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center" style="color:var(--ink-muted)">Belum ada akun.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{$pengguna->links()}}</div></div>
@endsection
