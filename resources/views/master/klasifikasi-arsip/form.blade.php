@extends('layouts.app')
@section('title',$klasifikasi->exists?'Ubah Klasifikasi':'Tambah Klasifikasi')
@section('content')
<div class="w-full max-w-3xl"><form method="POST" action="{{$klasifikasi->exists?route('klasifikasi-arsip.update',$klasifikasi):route('klasifikasi-arsip.store')}}" class="bg-white rounded-2xl border border-gray-200 p-6 space-y-5">@csrf @if($klasifikasi->exists) @method('PUT') @endif @if($errors->any())<div class="rounded-xl bg-red-50 border border-red-200 text-red-700 px-4 py-3 text-sm">@foreach($errors->all() as $e)<p>{{$e}}</p>@endforeach</div>@endif
<div><label class="block text-sm font-medium mb-1.5">Kode Klasifikasi</label><input name="kode_klasifikasi" value="{{old('kode_klasifikasi',$klasifikasi->kode_klasifikasi)}}" class="w-full rounded-xl border border-gray-300 px-3 py-3" required></div><div><label class="block text-sm font-medium mb-1.5">Nama Klasifikasi</label><input name="nama_klasifikasi" value="{{old('nama_klasifikasi',$klasifikasi->nama_klasifikasi)}}" class="w-full rounded-xl border border-gray-300 px-3 py-3" required></div><div><label class="block text-sm font-medium mb-1.5">Parent</label><select name="parent_id" class="w-full rounded-xl border border-gray-300 px-3 py-3"><option value="">-- Root --</option>@foreach($parentList as $p)<option value="{{$p->id_klasifikasi}}" {{(string)old("parent_id",$klasifikasi->parent_id)===(string)$p->id_klasifikasi?"selected":""}}>{{$p->kode_klasifikasi}} - {{$p->nama_klasifikasi}}</option>@endforeach</select></div><div class="flex justify-end gap-3"><a href="{{route('klasifikasi-arsip.index')}}" class="px-5 py-2.5 rounded-xl border border-gray-300 text-sm">Batal</a><button class="px-5 py-2.5 rounded-xl text-white text-sm" style="background:var(--navy)">Simpan</button></div></form></div>
@endsection
